<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:AppTracker"
             x:Class="AppTracker.MainPage">
    <ContentPage.Content>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" /> 
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button x:Name="app1" BackgroundColor="#eee" Text="{Binding APP1NAME}" Grid.Row="0" Grid.Column="0" />
            <Button x:Name="app2" BackgroundColor="#eee" Text="{Binding APP2NAME}" Grid.Row="0" Grid.Column="1" />
            <Button x:Name="app3" BackgroundColor="#eee" Text="{Binding APP3NAME}" Grid.Row="1" Grid.Column="0" />
            <Button x:Name="app4" BackgroundColor="#eee" Text="{Binding APP4NAME}" Grid.Row="1" Grid.Column="1" />
            <Button x:Name="app5" BackgroundColor="#eee" Text="{Binding APP5NAME}" Grid.Row="2" Grid.Column="0" />
            <Button x:Name="app6" BackgroundColor="#eee" Text="{Binding APP6NAME}" Grid.Row="2" Grid.Column="1" />
        </Grid>
    </ContentPage.Content>
</ContentPage>


